% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dirichlet.R
\name{plot_dirichlet_actions}
\alias{plot_dirichlet_actions}
\title{Plot BMA-Optimal Action Frequencies from Dirichlet Sensitivity Analysis}
\usage{
plot_dirichlet_actions(
  dirichlet_result,
  title = "Weighted-Optimal Action across Dirichlet Weight Draws",
  decision_order = NULL
)
}
\arguments{
\item{dirichlet_result}{A single list returned by \code{\link[=compute_evca_dirichlet]{compute_evca_dirichlet()}},
or a named list of such results.}

\item{title}{Plot title. Default: "BMA-Optimal Action across Dirichlet Weight
Draws".}

\item{decision_order}{Optional character vector specifying the stacking order
of decisions in the bars (default: order found in action_frequencies).}
}
\value{
A ggplot object that can be further customized or printed.
}
\description{
Creates a stacked bar chart showing what fraction of Dirichlet weight draws
result in each action being weighted-optimal. Supports a single result or a named
list of results for multi-scenario comparison.
}
\examples{
model_utilities <- matrix(
  c(
     5, 40, 80,
    90, 82,  8,
    72, 78, 55
  ),
  nrow = 3, ncol = 3, byrow = TRUE,
  dimnames = list(
    c("No road", "Direct route", "Rerouted road"),
    c("Officials", "ProDev_Hmong", "Traditional_Hmong")
  )
)
norm_col <- function(x) (x - min(x)) / (max(x) - min(x))
U_norm <- apply(model_utilities, 2, norm_col)

r1 <- compute_evca_dirichlet(U_norm, alpha = c(2, 2, 2), seed = 42)
r2 <- compute_evca_dirichlet(U_norm, alpha = c(8, 8, 2), seed = 42)
plot_dirichlet_actions(
  list("Equal (2,2,2)" = r1, "Dev-coalition (8,8,2)" = r2),
  decision_order = c("No road", "Direct route", "Rerouted road")
)
}
\seealso{
\code{\link[=compute_evca_dirichlet]{compute_evca_dirichlet()}}, \code{\link[=plot_dirichlet_evca]{plot_dirichlet_evca()}}
}
\keyword{decision-analysis}
\keyword{dirichlet}
\keyword{sensitivity}
\keyword{visualization}
